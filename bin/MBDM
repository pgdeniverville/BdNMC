#!/bin/bash
set -e

START=$(date +%s)
cd Source
make main
if (($# != 0))
then
    for arg in "$@"
    do
        ./main $arg
    done
else
    ./main
fi
cd ..
END=$(date +%s)
DIFF=$(( $END - $START ))
echo "Execution time = $DIFF s"
